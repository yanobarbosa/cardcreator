import type { Card, CardTemplate, TemplateLayer } from '@card-forge/core';
const escape=(value:unknown)=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]!));
export const fabClassicTemplate:CardTemplate={id:'fab-classic',name:'Classic placeholder',rulesetId:'fab',width:630,height:880,background:'#243763',layers:[{id:'frame',type:'shape',x:0,y:0,width:100,height:100,zIndex:0,visible:true},{id:'art',type:'artwork',x:7,y:10,width:86,height:35,zIndex:1,visible:true},{id:'name',type:'text',binding:'name',x:9,y:5,width:82,height:5,zIndex:2,visible:true,style:{fontSize:25,fontWeight:800}},{id:'type',type:'text',binding:'typeLine',x:9,y:47,width:82,height:5,zIndex:2,visible:true},{id:'stats',type:'stat',binding:'stats',x:9,y:54,width:82,height:7,zIndex:2,visible:true},{id:'rules',type:'text',binding:'rulesText',x:9,y:64,width:82,height:22,zIndex:2,visible:true},{id:'footer',type:'text',binding:'footer',x:9,y:91,width:82,height:4,zIndex:2,visible:true}]};
export function renderCardSvg(card:Card,template:CardTemplate):string { const stats=[['Pitch',card.pitch],['Cost',card.cost],['Power',card.power],['Defense',card.defense],['Life',card.life],['Intellect',card.intellect]].filter(([,v])=>v!==undefined).map(([k,v])=>`${k}: ${v}`).join('   '); const value=(b:TemplateLayer['binding'])=>b==='name'?card.name:b==='typeLine'?[card.type,...card.subtypes].filter(Boolean).join(' — '):b==='stats'?stats:b==='rulesText'?card.rulesText:[card.rarity,card.setId,card.collectorNumber,card.artist].filter(Boolean).join(' · '); const layers=[...template.layers].sort((a,b)=>a.zIndex-b.zIndex).filter(l=>l.visible).map(l=>{const x=l.x/100*template.width,y=l.y/100*template.height,w=l.width/100*template.width,h=l.height/100*template.height;if(l.type==='shape')return `<rect width="${template.width}" height="${template.height}" rx="28" fill="${template.background}" stroke="#d8b86c" stroke-width="14"/>`;if(l.type==='artwork'){if(!card.artwork?.source)return `<rect x="${x}" y="${y}" width="${w}" height="${h}" fill="#121a35"/><text x="${x+w/2}" y="${y+h/2}" text-anchor="middle" fill="#aab6d9">Artwork placeholder</text>`;const a=card.artwork;return `<image href="${escape(a.source)}" x="${x+a.positionX*w/100}" y="${y+a.positionY*h/100}" width="${w*a.scale}" height="${h*a.scale}" preserveAspectRatio="xMidYMid ${a.cropMode==='contain'?'meet':'slice'}" transform="rotate(${a.rotation} ${x+w/2} ${y+h/2})"/>`;}const text=value(l.binding);return text?`<foreignObject x="${x}" y="${y}" width="${w}" height="${h}"><div xmlns="http://www.w3.org/1999/xhtml" style="color:white;font-family:system-ui;font-size:${l.style?.fontSize??16}px;font-weight:${l.style?.fontWeight??400};white-space:pre-wrap;line-height:1.3">${escape(text)}</div></foreignObject>`:'';}).join('');return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${template.width} ${template.height}" role="img" aria-label="${escape(card.name)}">${layers}</svg>`; }
